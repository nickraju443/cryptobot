flask>=2.3
numpy>=1.24
pandas>=2.0
requests>=2.31
xgboost>=2.0
lightgbm>=4.0
scikit-learn>=1.3
ccxt>=4.0
# Browser automation for andX execution (the website's /p/v1/order/instant_order/
# endpoint requires a JS-derived access-sign that can't be computed from API
# credentials, so we drive a persistent Chromium session that's logged in).
# After install: python -m playwright install chromium
playwright>=1.60
